name: Test Secrets Access

on:
  workflow_dispatch:

jobs:
  test-secrets:
    runs-on: ubuntu-latest

    steps:
      - name: Test Secrets Access
        run: |
          echo "Testing secrets access..."

          # UPSTREAM secrets 테스트
          if [ -n "${{ secrets.UPSTREAM_OWNER }}" ]; then
            echo "✅ UPSTREAM_OWNER: Set (length: ${#UPSTREAM_OWNER})"
          else
            echo "❌ UPSTREAM_OWNER: Not set"
          fi

          if [ -n "${{ secrets.UPSTREAM_REPO }}" ]; then
            echo "✅ UPSTREAM_REPO: Set (length: ${#UPSTREAM_REPO})"
          else
            echo "❌ UPSTREAM_REPO: Not set"
          fi

          # Vercel secrets 테스트
          if [ -n "${{ secrets.VERCEL_TOKEN }}" ]; then
            echo "✅ VERCEL_TOKEN: Set (length: ${#VERCEL_TOKEN})"
          else
            echo "❌ VERCEL_TOKEN: Not set"
          fi

          if [ -n "${{ secrets.VERCEL_ORG_ID }}" ]; then
            echo "✅ VERCEL_ORG_ID: Set (length: ${#VERCEL_ORG_ID})"
          else
            echo "❌ VERCEL_ORG_ID: Not set"
          fi

          if [ -n "${{ secrets.VERCEL_PROJECT_ID }}" ]; then
            echo "✅ VERCEL_PROJECT_ID: Set (length: ${#VERCEL_PROJECT_ID})"
          else
            echo "❌ VERCEL_PROJECT_ID: Not set"
          fi

          # GitHub Token 테스트
          if [ -n "${{ secrets.GITHUB_TOKEN }}" ]; then
            echo "✅ GITHUB_TOKEN: Set (length: ${#GITHUB_TOKEN})"
          else
            echo "❌ GITHUB_TOKEN: Not set"
          fi

          echo "🎉 Secrets test completed!"
